#include "main.h"
/**
 * _abs - prints the absolute value of an integer
 * @c: parameter passed to the function
 * Return: returns the absolute value (success)
 */

int _abs(int c)
{
	if (c >= 0)
		return (c);
	else
		return (-c);
}
