#include "main.h"

/**
 * mul - multiplies two numbers
 * @a: an arguement passed to the function
 * @b: an argument passed to the function
 * Return: mul (result)
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul); /* return result */
}
